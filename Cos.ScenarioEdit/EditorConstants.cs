namespace ScenarioEdit
{
    internal class EditorConstants
    {
        /// <summary>"
        /// The action that will be performed when clicking the left and right mouse button.
        /// </summary>
        public enum PrimaryMode
        {
            Insert,
            Select,
            Shape
        }

        public enum ShapeFillMode
        {
            Insert,
            Delete,
            Select,
        }
    }
}
